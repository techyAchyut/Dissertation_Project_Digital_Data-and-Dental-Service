<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <button type="button" data-dismiss="modal" aria-hidden="true" class="close"><span
                    class="mdi mdi-close"></span></button>
        </div>
        <div class="modal-body">
            <form action="/admin/appointment/{{ appointment.id }}/delete" id="hospitalUpdate">
                <div class="text-center">
                    <div class="text-danger"><span class="modal-main-icon mdi mdi-close-circle-o"></span></div>
                    <h3>Are you sure you want to delete this?</h3>
                    <h4>Client : {{appointment.name}}</h4>
                    <h4>Date : {{date appointment.date}}</h4>
                    <div class="xs-mt-50">
                        <button type="button" data-dismiss="modal" class="btn btn-space btn-default">Cancel</button>
                        <button type="submit" class="btn btn-space btn-danger">Delete</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>